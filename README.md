# Prism

A personal build log. Track time across projects with a contribution grid where each project gets its own color.

## Stack

React · Vite · Supabase · Framer Motion

## Run locally

```bash
npm install
npm run dev
```

## Deploy

Vercel. Set `DEV_MODE = false` in `src/lib/supabase.js` before deploying.
